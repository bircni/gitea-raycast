{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "gitea",
  "title": "Gitea",
  "description": "Connects your Gitea instance to Raycast",
  "icon": "extension-icon.png",
  "license": "MIT",
  "commands": [
    {
      "name": "gitea-repositories",
      "title": "Gitea Repositories",
      "description": "Gives a list of repositories",
      "mode": "view",
      "subtitle": "Search and open Gitea repositories",
      "preferences": [
        {
          "name": "baseUrl",
          "title": "Gitea Base URL",
          "type": "textfield",
          "description": "Base URL of your Gitea instance",
          "required": true,
          "placeholder": "https://gitea.example.com"
        },
        {
          "name": "accessToken",
          "title": "Access Token",
          "type": "password",
          "description": "Optional personal access token for private repositories",
          "required": true,
          "placeholder": "Optional personal access token"
        },
        {
          "name": "cacheTtlMinutes",
          "title": "Cache TTL (minutes)",
          "type": "textfield",
          "description": "How long to cache repository results",
          "required": false,
          "default": "60"
        },
        {
          "name": "quickOpen",
          "title": "Quick Open Repository",
          "type": "checkbox",
          "label": "Open repositories directly instead of selecting a section",
          "description": "Skip the section picker when opening repositories",
          "required": false,
          "default": false
        }
      ]
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.103.0",
    "@raycast/utils": "^2.2.1"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^2.0.4",
    "@types/node": "22.13.10",
    "@types/react": "19.0.10",
    "eslint": "^9.22.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.2",
    "vitest": "^2.1.9"
  },
  "scripts": {
    "dev": "ray develop",
    "lint": "ray lint",
    "fix-lint": "ray lint --fix",
    "test": "vitest run",
    "build": "ray build",
    "publish": "npx @raycast/api@latest publish",
    "prepublishOnly": "echo \"\\n\\nIt seems like you are trying to publish the Raycast extension to npm.\\n\\nIf you did intend to publish it to npm, remove the \\`prepublishOnly\\` script and rerun \\`npm publish\\` again.\\nIf you wanted to publish it to the Raycast Store instead, use \\`npm run publish\\` instead.\\n\\n\" && exit 1"
  },
  "author": "bircni",
  "platforms": [
    "Windows",
    "macOS"
  ],
  "categories": [
    "Developer Tools"
  ]
}
